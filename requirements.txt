# 核心机器学习框架
torch>=2.0.0

# AWS 服务（Rekognition, S3）
boto3>=1.28.0
#
# 说明：当本机 AWS 配置使用 SSO/Identity Center 等 “login credential provider” 时，
# botocore 需要额外的 crt 依赖，否则会报 MissingDependencyException。
botocore[crt]>=1.28.0

# Google Cloud 服务（Video Intelligence, Vertex AI, Storage）
google-cloud-storage>=2.10.0
google-cloud-videointelligence>=2.16.0
google-cloud-aiplatform>=1.38.0

# Aliyun OSS 服务
oss2>=2.18.0

# Aliyun 百炼平台服务（使用 OpenAI 兼容接口）
# 注意：openai 库已经在 OpenAI 服务中列出，这里不需要重复添加

# OpenAI 服务
openai>=1.0.0

# 数据处理工具
pandas>=2.0.0

# 进度条工具
tqdm>=4.65.0

# HTTP 请求库（用于 Cloud Functions 调用）
requests>=2.31.0
